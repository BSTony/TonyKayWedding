{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "players": {
      ".indexOn": ["points", "lastSeen", "createdAt"]
    },
    "rankedQueue": {
      ".indexOn": ["createdAt", "status"]
    },
    "rankedRooms": {
      ".indexOn": ["createdAt", "status"]
    }
  }
}
